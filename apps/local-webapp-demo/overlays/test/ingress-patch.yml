apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-live
spec:
  tls:
  - hosts:
    - test.patagium.eu
    secretName: test-patagium-eu-tls
  rules:
  - host: test.patagium.eu
